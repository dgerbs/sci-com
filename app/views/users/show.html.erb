<% provide(:title, @user) %>

<div class="row">
  <div class="col-xs-12 col-md-6 col-md-offset-3">
    <section>
      <h1>
        <%= @user.email %>
      </h1>
    </section>
    <div class="stats">
      <%= render 'users/stats' %>
      <%= render 'users/follow_form' if signed_in? %>
    </div>  
  </div>
</div>
