<table class="table table-striped">
  <thead>
    <tr>
      <th>By</th>
    </tr>
  </thead>

  <tbody>
   <% @ibprotocols.each do |protocol| %>
    <tr>
      <td>
        <% if current_user == protocol.user %>
          You
        <% else %>
          <%= link_to protocol.user.email, protocol.user %>
        <% end %>
      </td>
      <td><%= link_to 'Show', protocol, class: "btn-link" %></td>
    </tr>
  <% end %>
  </tbody>
</table>